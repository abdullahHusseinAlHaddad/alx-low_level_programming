#include <stdio.h>

/**
 * main - Prints program name, followed by new line.
 * @argc: number.
 * @argv: array.
 * Return: 0
 */
int main(int argc, char const *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
